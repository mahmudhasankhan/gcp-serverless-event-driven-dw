steps:
  # Step 1 — Build the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-f"
      - "Dockerfile.marts-test"
      - "-t"
      - "gcr.io/$PROJECT_ID/dbt-marts-test"
      - "."

  # Step 2 — Push image to Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/dbt-marts-test"

  # Step 3 — Deploy as a Cloud Run Job
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      - "run"
      - "jobs"
      - "deploy"
      - "dbt-marts-test-job"
      - "--image=gcr.io/$PROJECT_ID/dbt-marts-test"
      - "--region=asia-south2"
      - "--memory=2Gi"
      - "--cpu=1"
      - "--max-retries=1"
      - "--task-timeout=1800"
      - "--service-account=bigquery-service@sales-datawarehouse.iam.gserviceaccount.com"
      - "--set-env-vars=DBT_PROJECT=sales-datawarehouse,DBT_DATASET=staging,DBT_LOCATION=asia-south2"

images:
  - "gcr.io/$PROJECT_ID/dbt-marts-test"